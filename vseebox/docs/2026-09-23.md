# Initial lab discovery — 2026-09-23

## Observed

- Current host: `sean-OptiPlex-3080`.
- Ethernet: `enp2s0`, UP, `192.168.50.1/24`.
- NetworkManager connection `Profile 1`: IPv4 method `shared`.
- Kernel IPv4 forwarding: `1`.
- Wi-Fi uplink: `wlx9cefd5fb400a`; default route through that interface.
- Ethernet neighbor `192.168.50.130` was REACHABLE. It is a candidate vSeeBox, not yet independently identified. Another neighbor, `.240`, was FAILED.
- Tailscale lists Dellmicro online at `100.92.219.66`.
- Optiplex has `/usr/bin/tcpdump` and `/usr/bin/git`. `gh`, `tshark`, `adb`, and `nmap` were not found by `command -v` in the current PATH.
- `/home/sean/.git` points to `git@github.com:sdriscoll97/Reverse_Engineering_VSeeBox.git`. Branch `main` has no commits; the existing top-level README is already staged. Other home-directory content is untracked.

## Tests performed and results

1. `ip -brief address`, `ip route`, `ip neigh show dev enp2s0`: established the interface and candidate neighbor above.
2. `nmcli -g ipv4.method connection show 'Profile 1'` and `cat /proc/sys/net/ipv4/ip_forward`: returned `shared` and `1`. This supports the expected routed/shared topology but does not prove successful Internet playback.
3. `tailscale status`: Dellmicro online.
4. SSH to `sean@100.92.219.66`, batch mode, ten-second connection timeout: initial host-key verification failed. Retried using `StrictHostKeyChecking=accept-new`; a first-use ED25519 host key was stored, but authentication failed with `Permission denied (publickey,password)`. No remote commands ran; tool inventory remains unknown.
5. `sudo -n timeout 20 tcpdump -i enp2s0 -nn -c 30 -s 96 'host 192.168.50.130'`: failed because sudo requires a password. No packets were captured.
6. `git ls-remote git@github.com:sdriscoll97/Reverse_Engineering_VSeeBox.git`: failed SSH host-key verification; remote contents and write access remain unverified.

## Findings and next evidence needed

There is no evidence yet identifying streaming domains, media protocols, sports services, encryption behavior, or stream URLs. Required next steps: confirm the candidate device IP, enable an appropriate Dellmicro SSH login, and execute a privileged bounded capture on the Optiplex. Then compare idle/app-open/playback traces using Dellmicro's actual installed tools.

## Local changes

Added a bounded device-filtered capture script, command-construction tests, experiment instructions, and these findings. Networking configuration was not modified. The Dellmicro SSH host key was added to local known_hosts during first-use connection. Raw capture files are excluded from version control.

## Authentication follow-up

Verbose SSH diagnostics confirm Dellmicro offers both `publickey,password`. The Optiplex SSH agent has no identities, and none of the default private-key paths exists. Batch mode did not submit a password, so this result is not evidence of an incorrect or changed password. Tailscale status did not advertise SSH host keys for Dellmicro. An interactive authenticated SSH control connection can provide access without sharing a password in chat.

Local validation: `python3 -m unittest discover -s /home/sean/vseebox/tests -v` passed all three tests (target filter, duration bounds, and input validation). No live packet capture or remote tool inventory has succeeded yet.

## Authenticated Dellmicro access

The user opened an interactive SSH control connection at `/home/sean/.ssh/dellmicro-control`. Reusing that connection succeeded and returned hostname `dellmicro`. `/usr/bin/tcpdump` is in the remote PATH. The search located `/home/sean/ghidra-mcp`, its built `GhidraMCP-7.0.0.jar` and extension ZIP, and a user service definition at `~/.config/systemd/user/ghidra-mcp.service`; these paths establish files exist, not that the service is operational. Other queried tools (tshark, adb, jadx, apktool, frida, mitmproxy, nmap) were not found in the non-interactive PATH.

Optiplex still requires interactive sudo for capture; tcpdump has no file capabilities reported by getcap. Candidate device `192.168.50.130` remains REACHABLE. The user was asked to begin sports playback and record the app/channel. No traffic has been captured yet.

## First playback capture initiated by user

After unplugging/reconnecting the box, the Ethernet neighbor remained `192.168.50.130` with the same link-layer address, and `enp2s0` remained UP. Observed the user's capture process running at elapsed 87 seconds of its requested 120 seconds, filtering `host 192.168.50.130`. Output path: `private-captures/20260923T201632.646349Z/traffic.pcap`. A follow-up process check after the expected finish showed no capture processes. Successful completion, packet count, and file contents remain unverified pending access to the root-owned private capture directory. No inference about stream protocol is justified yet.
